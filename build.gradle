apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
     jcenter() // faster than: maven { url "http://repo.maven.apache.org/maven2" }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version:'4.12'
}
